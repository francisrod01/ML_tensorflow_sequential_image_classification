# ML_tensorflow_sequential_image_classification
This tutorial shows how to classify images using `tf.keras.Sequential`, convert to Tensorflow Lite model on-device ML on mobile, embedded, and IoT devices.
